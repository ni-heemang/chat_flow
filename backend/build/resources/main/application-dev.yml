# 개발 환경 설정
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowchat_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 
    
  jpa:
    hibernate:
      ddl-auto: create-drop # 개발 시 테이블 자동 생성/삭제
    show-sql: true
    
logging:
  level:
    com.flowchat: DEBUG
    root: INFO